import borderRadius from './tokens/borderRadius';
import buttons from './tokens/buttons';
import colors from './tokens/colors';
import header from './tokens/header';

export default {
  borderRadius,
  buttons,
  colors,
  header,
};
